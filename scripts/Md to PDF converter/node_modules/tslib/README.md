---
source-git-commit: a517442244806bc6aef0f5bfb165c5d4f67341be
workflow-type: tm+mt
source-wordcount: '273'
ht-degree: 1%

---
# tslib

这是[TypeScript](https://www.typescriptlang.org/)的运行时库，包含所有TypeScript帮助程序函数。

此库主要由TypeScript中的`--importHelpers`标志使用。
使用`--importHelpers`时，在下列发出的文件中使用帮助器函数（如`__extends`和`__assign`）的模块：

```ts
var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
exports.x = {};
exports.y = __assign({}, exports.x);
```

将按照如下方式发出：

```ts
var tslib_1 = require("tslib");
exports.x = {};
exports.y = tslib_1.__assign({}, exports.x);
```

由于这样可以避免重复声明`__extends`、`__assign`等，这意味着平均为用户传递较小的文件，同时减少运行时开销。
对于带有TypeScript的优化捆绑包，您绝对应该考虑使用`tslib`和`--importHelpers`。

# 安装

对于最新的稳定版本，请运行：

## npm

```sh
# TypeScript 3.9.2 or later
npm install tslib

# TypeScript 3.8.4 or earlier
npm install tslib@^1

# TypeScript 2.3.2 or earlier
npm install tslib@1.6.1
```

## 纱线

```sh
# TypeScript 3.9.2 or later
yarn add tslib

# TypeScript 3.8.4 or earlier
yarn add tslib@^1

# TypeScript 2.3.2 or earlier
yarn add tslib@1.6.1
```

## 鲍尔

```sh
# TypeScript 3.9.2 or later
bower install tslib

# TypeScript 3.8.4 or earlier
bower install tslib@^1

# TypeScript 2.3.2 or earlier
bower install tslib@1.6.1
```

## JSPM

```sh
# TypeScript 3.9.2 or later
jspm install tslib

# TypeScript 3.8.4 or earlier
jspm install tslib@^1

# TypeScript 2.3.2 or earlier
jspm install tslib@1.6.1
```

# 使用情况

在命令行上设置`importHelpers`编译器选项：

```
tsc --importHelpers file.ts
```

或在tsconfig.json中：

```json
{
    "compilerOptions": {
        "importHelpers": true
    }
}
```

#### 对于bower和JSPM用户

您将需要为`tslib`添加`paths`映射，例如，对于Bower用户：

```json
{
    "compilerOptions": {
        "module": "amd",
        "importHelpers": true,
        "baseUrl": "./",
        "paths": {
            "tslib" : ["bower_components/tslib/tslib.d.ts"]
        }
    }
}
```

对于JSPM用户：

```json
{
    "compilerOptions": {
        "module": "system",
        "importHelpers": true,
        "baseUrl": "./",
        "paths": {
            "tslib" : ["jspm_packages/npm/tslib@2.x.y/tslib.d.ts"]
        }
    }
}
```

## 部署

- 选择您的新版本号
- 在`package.json`和`bower.json`中设置
- 创建标签： `git tag [version]`
- 推送标记： `git push --tags`
- 在GitHub中创建[授权声明](https://github.com/microsoft/tslib/releases)
- 运行[发布到npm](https://github.com/microsoft/tslib/actions?query=workflow%3A%22Publish+to+NPM%22)工作流

完成。

# Contribute

[向TypeScript添加](https://github.com/Microsoft/TypeScript/blob/master/CONTRIBUTING.md)的方法有很多。

* [提交错误](https://github.com/Microsoft/TypeScript/issues)，并在签入时帮助我们验证修复。
* 查看[源代码更改](https://github.com/Microsoft/TypeScript/pulls)。
* 在[StackOverflow](http://stackoverflow.com/questions/tagged/typescript)上与其他TypeScript用户和开发人员联系。
* 在Twitter上加入[#typescript](http://twitter.com/#!/search/realtime/%23typescript)讨论。
* [Contribute错误修复](https://github.com/Microsoft/TypeScript/blob/master/CONTRIBUTING.md)。

# 文档

* [快速教程](http://www.typescriptlang.org/Tutorial)
* [《方案编制手册》](http://www.typescriptlang.org/Handbook)
* [主页](http://www.typescriptlang.org/)
