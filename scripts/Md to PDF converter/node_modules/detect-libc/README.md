---
source-git-commit: a517442244806bc6aef0f5bfb165c5d4f67341be
workflow-type: tm+mt
source-wordcount: '274'
ht-degree: 0%

---
# detect-libc

用于检测C标准库(libc)详细信息的Node.js模块
实现由给定的Linux系统提供。

目前支持GNU glibc和MUSL libc的检测。

提供异步和同步功能
系列（例如`glibc`、`musl`）和版本（例如`1.23`、`1.2.3`）。

libc实现的版本号
不保证符合半导体规范。

有关以前的v1.x版本，请参见
[v1](https://github.com/lovell/detect-libc/tree/v1)分支。

## 安装

```sh
npm install detect-libc
```

## API

### GLIBC

```ts
const GLIBC: string = 'glibc';
```

包含值`glibc`的字符串常量。

### MUSL

```ts
const MUSL: string = 'musl';
```

包含值`musl`的字符串常量。

### 家庭

```ts
function family(): Promise<string | null>;
```

与以下各项异步解决：

* 确定libc系列时`glibc`或`musl`
* 无法确定libc系列时`null`
* 在非Linux平台上运行时`null`

```js
const { family, GLIBC, MUSL } = require('detect-libc');

switch (await family()) {
  case GLIBC: ...
  case MUSL: ...
  case null: ...
}
```

### familySync

```ts
function familySync(): string | null;
```

`family()`的同步版本。

```js
const { familySync, GLIBC, MUSL } = require('detect-libc');

switch (familySync()) {
  case GLIBC: ...
  case MUSL: ...
  case null: ...
}
```

### 版本

```ts
function version(): Promise<string | null>;
```

与以下各项异步解决：

* 可以确定的版本
* 无法确定libc系列时`null`
* 在非Linux平台上运行时`null`

```js
const { version } = require('detect-libc');

const v = await version();
if (v) {
  const [major, minor, patch] = v.split('.');
}
```

### versionSync

```ts
function versionSync(): string | null;
```

`version()`的同步版本。

```js
const { versionSync } = require('detect-libc');

const v = versionSync();
if (v) {
  const [major, minor, patch] = v.split('.');
}
```

### isNonGlibcLinux

```ts
function isNonGlibcLinux(): Promise<boolean>;
```

与以下各项异步解决：

* 当libc系列为`glibc`时，`false`
* 当libc系列不是`glibc`时，`true`
* 在非Linux平台上运行时`false`

```js
const { isNonGlibcLinux } = require('detect-libc');

if (await isNonGlibcLinux()) { ... }
```

### isNonGlibcLinuxSync

```ts
function isNonGlibcLinuxSync(): boolean;
```

`isNonGlibcLinux()`的同步版本。

```js
const { isNonGlibcLinuxSync } = require('detect-libc');

if (isNonGlibcLinuxSync()) { ... }
```

## 许可

版权所有2017 Lovell Fuller和其他人。

根据Apache许可证版本2.0（以下简称“许可证”）授予许可；
除非遵守许可证规定，否则您不得使用此文件。
您可以在[http://www.apache.org/licenses/LICENSE-2.0](http://www.apache.org/licenses/LICENSE-2.0.html)获取许可证副本

除非适用法律要求或书面同意，否则软件
按“原样”进行分发，
无任何明示或暗示的担保或条件。
有关管理权限的特定语言的信息，请参阅许可证，然后
许可下的限制。
